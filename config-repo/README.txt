This folder contains sample Spring Cloud Config repository files.
These configs are intended for a Config Server demo setup.

Included examples:
- application.yml: global management endpoint exposure (health, info)
- user-service.yml: tomcat max threads and greeting
- order-service.yml: order timeout and retry settings
- api-gateway.yml: gateway HTTP client connect-timeout
